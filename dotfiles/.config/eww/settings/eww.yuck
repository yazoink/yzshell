(defvar settings_menu_item 0)

(include "settings/widgets/wallpaper.yuck")
(include "settings/widgets/colours.yuck")

(defwidget settings_menu_button [index icon tooltip text class]
    (button
      :onclick "${EWW_CMD} update settings_menu_item=${index}"
      :tooltip tooltip
      :class "${class} ${settings_menu_item == index ? "selected" : ""}"
      (box
        :orientation "h"
        :space-evenly false
        (label :text icon :class "icon")
        (revealer
          :transition "slideright"
          :reveal {settings_menu_item == index ? true : false}
          (label :text text)
        )
      )
    )
)

(defwindow settings
  :monitor 0
  :geometry (geometry
    :x 4
    :y 4
    :anchor "bottom center"
  )
  :stacking "fg"
  :exclusive false
  :focusable "ondemand"
  (box
    ;:visible settings_visible
    :orientation "v"
    :class "settings-box"
    :space-evenly false
    :vexpand false
    :spacing 8
    (box
      :class "settings-menu"
      :orientation "h"
      :halign "fill"
      :space-evenly true
      :hexpand true
      (box
        :orientation "h"
        :halign "center"
        :space-evenly false
        :spacing 8
        (settings_menu_button
          :index 0
          :icon ""
          :tooltip "Wallpaper settings"
          :text "Wallpaper"
          :class "wallpaper"
        )
        (settings_menu_button
          :index 1
          :icon ""
          :tooltip "Colourscheme settings"
          :text "Colourscheme"
          :class "colours"
        )
      )
    )
    (stack
      :selected settings_menu_item
      :class "settings-content"
      :same-size true
      :transition {settings_menu_item == 0 ? "slideleft" : "slideright"}
      (wallpaper)
      (colourschemes)
    )
  )
)
